export enum MetricType {
  REPS = 'reps', // e.g. Bench Press, Pull-ups
  WEIGHT = 'weight', // e.g. Deadlift, Squat (used with reps)
  DISTANCE = 'distance', // e.g. Running, Rowing
  DURATION = 'duration', // e.g. Plank, Stretch
  CALORIES = 'calories', // e.g. Cardio machines
  LEVEL = 'level', // e.g. skill-based or resistance-band levels
}
